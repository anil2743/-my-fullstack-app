# Server Port
server.port=8080
server.servlet.context-path=/api

# Database Connection
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/bankingdb}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Anilp@2024}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# AES Encryption Key
aes.encryption.key=${AES_KEY:MySuperSecretKey}

# JWT Secret Key
jwt.secret=${JWT_SECRET:mySecretKeyWhichIsAtLeast256BitsLongForHS256Algorithm}

# Encryption Configuration
encryption.key=${ENCRYPTION_KEY:MySecretKey12345}

# Logging
logging.level.com.example.demo=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Error Handling
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=never
server.error.include-exception=false

# CORS Configuration
spring.mvc.cors.enabled=true

# SSL Configuration (handled by Nginx)
server.ssl.enabled=false
server.forward-headers-strategy=NATIVE